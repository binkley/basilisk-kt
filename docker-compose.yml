version: '3.1'

services:
  basilisk-db:
    environment:
      - POSTGRES_USER=basilisk
    image: postgres:10.9-alpine
    ports:
      - 5432:5432
    volumes:
      - ./initdb:/docker-entrypoint-initdb.d

  basilisk-service:
    depends_on:
      - basilisk-db
    image: basilisk
    ports:
      - 8080:8080
